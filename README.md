## Setup bedrock 
- /setup-bedrock

  ```
  {
      "env": {
      "CLAUDE_CODE_USE_BEDROCK": "1",
      "AWS_REGION": "us-east-1",
      "AWS_PROFILE": "aws-profile"
    },
    "theme": "dark"
  }
  ```
